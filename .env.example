# Frontend build-time variable
VITE_API_URL=https://your-fitproof-worker.workers.dev

# Worker variables (configure in Cloudflare, not in the browser build)
ALLOWED_ORIGIN=https://your-mini-app.example
TREASURY_ADDRESS=NQ...
# TREASURY_PRIVATE_KEY=server-only-secret
