import { describe, expect, it } from 'vitest';
import { issueChallenge, maskWallet, verifyChallenge, Session } from './proof';
const session: Session = {sessionId:'s1',wallet:'NQxx123456789',questId:'circuit-today',dayKey:'2026-09-04',sessionHash:'hash',durationSec:480,completed:true};
describe('proof verification', () => { it('rejects incomplete sessions', () => expect(() => issueChallenge({...session, completed:false})).toThrow('incomplete')); it('rejects expired challenges', () => { const c=issueChallenge(session,1000); expect(verifyChallenge(c,'sig',session.wallet,301001)).toBe('expired'); }); it('rejects replayed challenges', () => { const c={...issueChallenge(session),usedAt:Date.now()}; expect(verifyChallenge(c,'sig',session.wallet)).toBe('replay'); }); it('rejects another wallet', () => expect(verifyChallenge(issueChallenge(session),'sig','NQother')).toBe('wallet-mismatch')); it('rejects a second daily receipt', () => { const c=issueChallenge(session); expect(verifyChallenge(c,'sig',session.wallet,Date.now(),[{...c,receiptId:'r',signature:'x',publicKey:session.wallet,pointsAwarded:10,createdAt:Date.now()}])).toBe('duplicate'); }); });
it('masks wallet identity', () => expect(maskWallet('NQxx123456789')).toBe('NQxx1…6789'));
